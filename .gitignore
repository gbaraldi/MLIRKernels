# Top-level Manifest pins exact versions; downstream users resolve their own.
# Subdir Manifests (test/, bench/) follow the same convention.
/Manifest.toml
test/Manifest.toml
bench/Manifest.toml

# LLVM IR dumps (regeneratable via bench/dump_llvm.jl)
bench/llvm_dump/

# Julia coverage/profiling
*.cov
*.mem
*.jlcov

# Editor
*.swp
*~
.DS_Store
